VITE_MAPBOX_TOKEN=pk.your_token_here

# Local dev: call backend directly
VITE_API_BASE_URL=http://localhost:8000

# Netlify production: use /api (proxied to backend via netlify.toml).
# Do NOT set http:// here — browsers block mixed content on HTTPS sites.
# VITE_API_BASE_URL=/api
